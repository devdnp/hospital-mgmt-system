package com.devdnp.hms.services;

import com.devdnp.hms.config.StaffInfoStaffDetails;
import com.devdnp.hms.entities.StaffMaster;
import com.devdnp.hms.repositories.StaffRepo;
import org.springframework.beans.factory.annotation.Autowired;
/*
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.core.userdetails.UsernameNotFoundException;


 */
import org.springframework.stereotype.Component;

import java.util.Optional;

//@Component
public class StaffMasterDetailsService
        //implements UserDetailsService
        {
/*
    @Autowired
    private StaffRepo staffRepo;

    @Override
    public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
        Optional<StaffMaster> userInfo = staffRepo.findByName(username);

//        return userInfo.map(StaffInfoStaffDetails::new)
//                .orElseThrow(()->new UsernameNotFoundException("User not Found"+username));

        return null;

    }


 */
}
